import { BaseCommonPipe } from './base-common.pipe';

describe('BaseCommonPipe', () => {
  it('create an instance', () => {
    const pipe = new BaseCommonPipe();
    expect(pipe).toBeTruthy();
  });
});
