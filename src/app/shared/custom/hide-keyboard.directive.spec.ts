import { HideKeyboardDirective } from './hide-keyboard.directive';

describe('HideKeyboardDirective', () => {
  it('should create an instance', () => {
    const directive = new HideKeyboardDirective();
    expect(directive).toBeTruthy();
  });
});
