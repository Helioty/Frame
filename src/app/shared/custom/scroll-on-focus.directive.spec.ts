import { ScrollOnFocusDirective } from './scroll-on-focus.directive';

describe('ScrollOnFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollOnFocusDirective();
    expect(directive).toBeTruthy();
  });
});
